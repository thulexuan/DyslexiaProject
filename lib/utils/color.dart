import 'dart:ui';

import 'package:flutter/material.dart';

const Map<int, Color> _creamColor = {
  50: Color.fromRGBO(255, 253, 208, 1),
};

const MaterialColor primaryColor = MaterialColor(0xfffdd0, _creamColor);
const textColorDefault = Colors.black;